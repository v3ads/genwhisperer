# The frontend is same-origin and needs no secrets.
# In dev, the Vite proxy forwards /api -> this target. Override if your
# backend runs elsewhere. (See vite.config.ts.)
VITE_DEV_API_TARGET=http://localhost:3001
